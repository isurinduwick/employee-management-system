// 10.0.2.2 is the Android emulator's alias for the host machine's localhost.
// A physical device needs the host's real LAN IP instead — swap this when
// testing on hardware.
const String apiBaseUrl = 'http://10.0.2.2:5133/api';
